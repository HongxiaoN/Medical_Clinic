<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.MedicalClinic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Medical_Clinic">
        <activity android:name=".PatientDashboardActivity"></activity>
        <activity android:name=".DoctorDashboardActivity" />
        <activity android:name=".Patient" />
        <activity android:name=".User" />
        <activity android:name=".Doctor" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".ListOfPatientActivity"
            android:exported="true" />
        <activity android:name=".AppointmentActivity" />
        <activity android:name=".DisplayDoctorActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>